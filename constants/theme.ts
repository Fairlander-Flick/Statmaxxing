// Legacy re-exports kept for backward compatibility.
// All new code should use `useTheme()` from lib/ThemeContext instead.
export { DARK_COLORS as COLORS, makeGlobalStyles as globalStyles } from '../lib/ThemeContext';
